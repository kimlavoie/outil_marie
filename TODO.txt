activities.js fait 1905 lignes et gère probablement le rendu de la liste — vérifier s'il y a un re-render complet du tableau à chaque frappe dans la recherche/filtre (debounce si ce n'est pas déjà fait).

Si le nombre d'activités grossit avec le temps, la pagination existe déjà mais vérifier que le tri/filtre n'itère pas sur appState.activities en entier à chaque interaction.

Recherche globale (activités + comptes + départements) au lieu de filtres séparés par vue

Aucun outil de build/lint/test visible — un simple ESLint + Prettier réduirait les risques de régression vu la taille des fichiers (settings.js à 1118 lignes, activities.js à 1905).

Pas de tests automatisés : au minimum des tests unitaires sur les calculs financiers (getRoomsTariffTotal, réconciliation, totaux du dashboard) puisque ce sont les fonctions les plus sensibles aux erreurs silencieuses.

Vérifier les contrastes et labels ARIA sur les formulaires (beaucoup de champs dans activities/settings) — pas testé ici sans lancer l'app

Le thème sombre/clair existe (applyTheme) — vérifier que le contraste est bon dans les deux