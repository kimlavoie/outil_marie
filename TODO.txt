TODO - Ameliorations Outil Marie
(genere le 2026-07-11 suite a une revue de code)

=======================================================
FIABILITE (priorite haute)
=======================================================

[x] Rollback automatique si la migration echoue pendant une restauration
    Fichier : src/services/backup/restore.ts (lignes 81-94)
    Deja en place (setAppState(preRestoreSnapshot) + toast d'erreur) -
    verifie le 2026-07-11, aucun changement necessaire.

[x] Garde-fous dans l'export Excel
    Fichier : src/services/excel-export.ts
    Ajout d'une verification que XLSX est bien charge et qu'au moins un
    compte est configure avant de generer le classeur (sinon message
    d'erreur clair au lieu d'un plantage/export vide).

[x] Montants invalides en reconciliation
    Fichier : src/services/reconciliation.ts
    Un "Montant courant" non numerique loggue desormais un avertissement
    (logWarn) au lieu d'etre silencieusement remplace par 0.
    Note : le regroupement par cle compte+reference est un comportement
    voulu (plusieurs transactions pour une meme facture doivent
    s'additionner), donc pas de "detection de doublons" a ajouter la.

=======================================================
TESTS (trous a combler)
=======================================================

[x] Ajouter des tests pour src/services/excel-export.ts
    test/excel-export.test.ts : garde-fous XLSX absent/comptes vides,
    filtrage par periode, formules de sommes, nom de fichier.
    (getExcelColName et runExportToExcel exportes pour etre testables,
    comme le fait deja contract-generator.ts pour ses helpers internes.)

[x] src/services/account-report.ts
    Deja teste (test/account-report.test.ts existait avant cette revue) -
    aucune action necessaire.

[x] src/services/backup/reminder.ts et backup/validation.ts
    getDaysSinceLastBackup, formatLocalDateToFrench, checkBackupReminder
    et validateBackupSchema (le point prioritaire, utilise lors d'une
    restauration) etaient deja couverts par test/backup/index.test.ts -
    aucune action necessaire. Non couverts (mineur, purement visuel) :
    renderBackupView, renderSafetyBackupsList, downloadSafetyBackup,
    exportDiagnosticLogs.

[x] Ajouter des tests pour src/activities/billing-tab.ts
    test/billing-tab.test.ts : calcul des lignes personnel/equipements/
    frais, lignes a montant nul omises, confirmation avant remplacement
    des lignes existantes.

[x] Ajouter des tests pour src/activities/file-links.ts
    test/file-links.test.ts : aller-retour IndexedDB des liens de fichier,
    rendu de renderFileLinkStatus (lie/non lie, etats Soumise/Approuvee),
    garde-fous de pickAndLinkFile (navigateur non compatible, fichier
    refuse pour le kind "form"). Non couvert (necessite l'API File System
    Access reelle, hors de portee de tests Node) : generateAndLinkFile,
    openLinkedFile, renderPdfPreview/renderXlsxPreview au-dela du cas
    "aucun fichier lie".

[x] Configurer un outil de couverture de tests (ex. c8) pour identifier
    objectivement les zones non testees
    Ajout de c8 (devDependency) + script "npm run test:coverage"
    (texte + rapport HTML dans coverage/, ignore par .gitignore).
    Couverture globale actuelle : ~70% des lignes de src/. Zones les
    plus faibles identifiees : src/activities/render.ts (22%),
    src/activities/form.ts (32%), src/services/backup/restore.ts (19%,
    la plupart des branches d'erreur/migration ne sont pas exercees),
    src/services/backup/index.ts (25%).

=======================================================
ARCHITECTURE (dette technique, non bloquant)
=======================================================

[x] Decouper src/services/contract-generator.ts (891 lignes)
    Nouveau dossier src/services/contract-generator/ : styles.ts (S,
    normalizeGridBorders), static-content.ts (textes fixes du contrat),
    sheet-builder.ts (SheetBuilder + helpers XML generiques),
    sheet-xml.ts (buildSheetXml/buildDrawingXml, la mise en page reelle).
    contract-generator.ts devient un barrel qui ne garde que
    l'assemblage du .xlsx (zip/OOXML) + generateContractXlsx/
    generateSoumissionXlsx, et re-exporte le reste pour ne rien casser
    (meme approche que backup/ et reservations/).

[x] Decouper src/activities/render.ts (735 lignes)
    Nouveaux fichiers src/activities/context-menu.ts (menu contextuel
    clic-droit sur une activite) et bulk-actions.ts (barre d'actions
    groupees). render.ts redevient un barrel qui re-exporte les deux et
    ne garde que l'etat/le rendu du tableau.

[x] Decouper src/utils/utils.ts (704 lignes, fourre-tout)
    Nouveaux fichiers src/utils/format.ts (formatage/masques),
    activity-helpers.ts (aides dependantes d'appState : references,
    salles, comptes GL), dom-helpers.ts (pagination, pill-toggle,
    overlay, toasts), select-helpers.ts (filtres multi-select),
    searchable-select.ts. utils.ts redevient un barrel qui re-exporte
    tout, aucun import existant a changer.

[x] Decouper src/navigation.ts (664 lignes)
    Nouveau dossier src/navigation/ : global-search.ts, quick-access.ts,
    period-selector.ts. navigation.ts redevient un barrel qui garde le
    theme/le switch de vue/l'orchestration du rendu et re-exporte le
    reste.

    Note commune aux 4 decoupages : les modules extraits importent
    certaines fonctions "en retour" du fichier barrel d'origine (import
    circulaire assume, deja utilise ailleurs dans le projet entre
    utils.ts et state.ts) - sans danger puisque rien ne s'execute au
    chargement des modules, seulement a l'appel des fonctions. Verifie
    apres coup : lint, typecheck, les 365 tests, et `npm run build`
    passent tous sans changement.

=======================================================
PRODUIT
=======================================================

[ ] Tracabilite utilisateur sur les decisions de reconciliation
    Fichier : src/services/reconciliation.ts
    Actuellement seul un timestamp est enregistre, pas l'identite de
    la personne ayant pris la decision.

[ ] Apercu avant telechargement pour la generation de contrat/soumission
    Fichier : src/services/contract-generator.ts

[ ] Sauvegarde automatique planifiee (au-dela du simple rappel)
    Fichier : src/services/backup/reminder.ts et auto-backup.ts

[x] Rendre les annees fiscales configurables (actuellement codees en dur)
    Fichier : src/navigation/period-selector.ts
    Remplace la liste figee 2024-2025..2029-2030 par une fenetre glissante
    calculee a partir de la date du jour (getFiscalYearWindow) : 1 an
    dans le passe, 3 ans dans le futur, autour de l'annee fiscale
    courante (getDefaultFiscalYear). Aucun reglage de configuration
    ajoute - la fenetre se decale automatiquement d'annee en annee.
    La fusion avec les annees deja utilisees par des activites existantes
    est conservee (une annee avec des donnees ne disparait jamais du
    menu meme hors fenetre). Teste dans test/period-selector.test.ts.

[x] "TODO" tarifs de salle obsoletes - fausse piste, rien a faire
    Fichier : src/activities/reservations/tariff.ts (ligne 83)
    Verifie le 2026-07-11 : ce n'est pas un TODO de travail inacheve,
    juste un commentaire qui se refere a lui-meme par le nom de la
    fonctionnalite. L'avertissement "Tarif obsolete" (comparaison entre
    le montant enregistre sur la reservation et ce que la grille
    tarifaire actuelle donnerait) est deja pleinement implemente dans
    updateResolvedPriceDisplay (lignes 70-96).
